<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.CollectMapper">
	<select id="selectByUid" parameterType="Long" resultType="Collect">
		select * from collect where uid = #{uid}
	</select>
	
	<select id="selectByNid" parameterType="Long" resultType="Novel">
		select * from novel where nid = #{nid}
	</select>
	
</mapper>